assembly-versioning-scheme: MajorMinorPatch
assembly-informational-format: '{MajorMinorPatch}.{CommitsSinceVersionSource}-{BranchName}'
mode: ContinuousDeployment
continuous-delivery-fallback-tag: build
branches:
  master: {}
ignore:
  sha: []